namespace HttpClientMock
{
    public enum ContentFormat
    {
        Json
    }

    public enum Behavior
    {
        Unspecified = 0,
        FakeAsSuccess = 1,
        FakeAsFailure = 2
    }
}
